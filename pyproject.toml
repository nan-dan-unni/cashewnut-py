[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cashewnut"
version = "0.3.0"
description = "A middleware-rich python framework for building high performance server side applications."
readme = "README.md"
requires-python = ">=3.8"
authors = [
    { name = "Nandanunni", email = "asnqln@gmail.com" }
]
